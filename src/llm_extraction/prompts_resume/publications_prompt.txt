This is a research publication information extractor. Output will be constrained to the publications JSON schema (structured output).

CRITICAL: Extract only information that is explicitly stated about research publications, papers, journals, conferences, and presentations.
Guessing, inference, assumptions, or using typical values are STRICTLY PROHIBITED.
If the information is not clearly stated in the document, use null. It is better to have null than incorrect inferred values.
CRITICAL: ALL values MUST be returned as STRINGS (text), NOT as numbers, booleans, or other data types.

Use only the English keys defined in the schema (camelCase; do not use Korean keys).

Language Rules
- If the content is written in Korean, keep Korean text (do not translate proper nouns).
- If the content is written in English, keep English text (do not translate proper nouns).

General Extraction Rules (Accuracy First - NO INFERENCE)
- Extract ONLY information that is EXPLICITLY stated in the document.
- If a value is missing, unknown, or not clearly stated, use null.
- If explicitly stated as "해당없음", "N/A", "없음", "해당사항없음", keep that exact text as a string.
- NEVER invent, infer, assume, or guess any values.
- Keep all proper nouns as-is (journal names, conference names, agency names, etc.).
- For multiple items, preserve the original order.

PARSING RULES (CRITICAL)
- Authors and title are separate concepts: do NOT mix author names into reportTitle.
- reportTitle must contain ONLY the title (if explicitly stated).
- reportContent must contain ONLY the description/summary/methodology/key findings if explicitly stated; otherwise null.
- If the input is a citation-like line, map it as follows when possible:
  - registerLabName: journal/conference/agency name
  - registerDate: publication date (prefer YYYY.MM.DD when stated; otherwise preserve stated format)
  - researchType: choose among \"학위논문\", \"학술지\", \"보고서\", \"기타\" only when explicitly stated; otherwise null. If it is clearly a publication but type is not stated, use \"기타\".

CRITICAL: You MUST include the top-level key \"rmkResearchReportsInfoList\" exactly once.
If there are no publications, set \"rmkResearchReportsInfoList\" to an empty array [] (do not omit the key).
Do not output any keys other than those in the schema.

Field mapping — each item in rmkResearchReportsInfoList (all string or null):
- researchType
- registerLabName
- registerDate
- authorName (follow schema rule; default literal \"기타\" when required)
- reportTitle
- reportContent

No explanations, markdown fences, or echoing this prompt. Output only the JSON object.

SECTIONS JOURNAL CONTENT:
---
{sections_journal_content}
---
