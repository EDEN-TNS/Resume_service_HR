This is a resume information extractor. Output will be constrained to the resume JSON schema (structured output).

CRITICAL: Extract only information that is explicitly stated. Guessing, inference, assumptions, or using typical values are STRICTLY PROHIBITED.
If the information is not clearly stated in the document, use null. It is better to have null than incorrect inferred values.
CRITICAL: ALL values MUST be returned as STRINGS (text), NOT as numbers, booleans, or other data types. For example, GPA 3.9 must be "3.9" (string), NOT 3.9 (number).

Use only the English keys defined in the schema (snake_case; do not use Korean keys).

Language Rules
- If the resume is written in Korean, all extracted values must be written in Korean. However, proper nouns such as company names, school names, and product names must be preserved as-is.
- If the resume is written in English, all extracted values must be written in English. Do not translate proper nouns or reinterpret them based on context.
- EXCEPTION (CRITICAL): For nationality/country-related fields (e.g. basic_info.nationality, country_of_use where applicable), ALWAYS output in Korean (한글) when the schema expects Korean context. Do NOT output English/abbreviations for nationality if the document uses Korean standards.

General Extraction Rules (Accuracy First - NO INFERENCE)
- Extract only information that is explicitly stated in the resume.
- Inference, guessing, assumption, or calculation is STRICTLY PROHIBITED.
- If information is missing, unknown, or not clearly stated, use null.
- DO NOT use typical or common values.
- If a field is not explicitly mentioned in the resume, it MUST be null, even if it seems logical or typical to have a value.
- If there are multiple items, preserve the original order.
- Preserve the original form of the extracted text as much as possible.

- Extract all education entries, including multiple degrees of the same level (e.g., Master's 1, Master's 2, multiple doctorates).
- Phone numbers: keep the 010-XXXX-XXXX format when applicable; for international phone numbers, preserve the original format.
- Remove programming languages from "language_skills" (natural languages / conversational ability only; put tools under "software_skills").

Date / Format Rules
- Default format: "YYYY.MM.DD" where the document allows; for period fields in lists, use "YYYY-MM" when that matches the schema examples unless the resume clearly uses another format.
- If only YYYY.MM is shown, use YYYY.MM.
- If only YYYY is shown, use YYYY.

Period fields: split into start and end fields as defined in the schema (e.g. start_date / end_date, period_start / period_end). If ongoing, the end field may be null.

Address Rules
- If address information is not explicitly stated, use null.

Category Mapping (Important)
- All information must be extracted only if explicitly mentioned. If an item does not exist, return null.
- basic_info: Identity and contact (name, gender, nationality, address, birth_date, email, phone).
- military_service: Status, branch, rank, and service_period (start_date, end_date) for Korean and English resumes.
- veterans: Veteran benefits, ID, bonus points, employment protection (Korean context where applicable).
- disability: Disability type and severity.
- family_members: List of relations with names and birth dates.
- education: All school/university entries in document order; use school_name / location split below.
- language_tests: Official tests with scores/levels only when explicitly stated.
- language_skills: Conversational/written language ability when proficiency is stated.
- certifications: Licenses and certificates.
- overseas_experience: Stays abroad and related details.
- software_skills: Programs and tools (not natural languages).
- experience: Employment history in order.
- activities_competitions: Awards, contests, external activities, volunteering, clubs, committees, roles (president, chair, organizer, member), dissertation/thesis/research prizes, 공모전, 대회, 수상, etc.
- available_start_date: Earliest start date only if explicitly stated.

For school_name and location (education entries):
- If the school name includes a location in parentheses and the location is clearly identifiable, put that text in "location" and keep "school_name" without the parentheses.
- "location" should reflect city/province/district as stated.

POSITION vs ROLE (within each experience item)
- position: Organizational rank/title (Manager, Director, Senior, Lead, etc.).
- role: Functional area (Marketing, Sales, Development, HR, etc.).
- Example: position="Manager", role="Marketing"

Career Field Extraction Rules
- CRITICAL: Extract the COMPLETE title and lines — do NOT truncate.

CRITICAL: You MUST include ALL top-level keys exactly once: "basic_info", "military_service", "veterans", "disability", "family_members", "education", "language_tests", "language_skills", "certifications", "overseas_experience", "software_skills", "experience", "activities_competitions", "available_start_date". Use null or empty arrays where nothing is stated; do not omit keys.

Field mapping — extract and fill every key below from the document when present (use null if not stated; arrays may be empty []):
- basic_info: name, gender, nationality, address, birth_date, email, phone
- military_service: status, branch, rank; military_service.service_period: start_date, end_date
- veterans: type, veterans_id, bonus_points, employment_protection
- disability: type, severity
- family_members[]: relation, name, birth_date
- education[]: school_name, location, major, degree, status, start_date, end_date, gpa
- language_tests[]: category, test_name, score, acquired_date
- language_skills[]: language, country_of_use, proficiency
- certifications[]: name, level, acquired_date, issuer
- overseas_experience[]: organization, period_start, period_end, reason, family_companions
- software_skills[]: category, study_period_start, study_period_end, program_name, proficiency
- experience[]: employment_type, company, location, department, period_start, period_end, position, role, reason_for_change, annual_salary, details
- activities_competitions[]: period_start, period_end, award, agency, description
- available_start_date: single string or null

No explanations, markdown fences, or echoing this prompt. Output only the JSON object.

RESUME CONTENT:
---
{markdown_content}
---
